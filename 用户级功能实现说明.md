# 用户级（非 admin / tom）功能核查与实现

> 场景：ENABLE_AUTH=true，以普通用户 **tom** 登录，核查与「用户个性化」相关的 API/UI 是否对非 admin 真正生效。

## 一、核查结论

| 功能 | 此前状态 | 本次处理 |
|---|---|---|
| **创建者记录（creator）** | 已实现：`model.py` 创建端点已 `instance_data['creator'] = current_user()` | 保持不变 |
| **修改者记录（modifier）** | 未实现 | **新增**：更新端点 stamp `current_user()`，仅 `host` 等含该列的表落库 |
| **属性列数据保存** | 前端已调 `saveModelCustomColumns`，但后端 `user.py` 读 `x-user-name` 请求头（前端从不发送）→ 所有列配置都被存到 **admin** 名下 | **修复**：后端改用 `current_user()`（由 Bearer token 服务端解析），tom 的配置正确按用户隔离 |
| **条件筛选用户级保存** | `filter-collection.vue` 用 `localStorage`，**无服务端落库**，且只在 host 列表挂载 | **重写**：服务端 `user_custom(filter_collection)` 存储、捕获当前激活条件、挂载到资源实例视图（bk_switch 等） |

## 二、改动文件

**后端**
- `cmdb_server_lite/app/api/v1/user.py`：`search_user_custom` / `save_user_custom` / `get_model_columns` / `save_model_columns` 4 处 `request.headers.get('x-user-name', 'admin')` → `current_user()`（来自 `app.auth.identity`，服务端从 token 解析，不再信任客户端头）。
- `cmdb_server_lite/app/api/v1/model.py`：`update_instance` 端点写入前 `instance_data['modifier'] = current_user()`（service 层按真实表列收敛，无 `modifier` 列的自定义模型自动跳过，不会报错）。

**前端**
- `cmdb_ui_lite/src/components/filters/filter-collection.vue`：`localStorage` 改为 `userCustom.saveUsercustom/searchUserCustom`（key=`filter_collection`）；新增 `currentConditions` / `objId` 两个 prop，收藏时深拷贝当前 conditionMap；补上原先缺失的 `@change="handleApply"` 绑定（原组件选中收藏项从不触发 apply）。
- `cmdb_ui_lite/src/views/general-model/index.vue`：选项栏挂载 `<filter-collection>`，新增 `handleCollectionApply` 将收藏的 conditionMap 还原为筛选并触发检索。

## 三、验证结果（tom 视角，端到端）

| 验证项 | 结果 |
|---|---|
| 列配置按用户隔离 | tom 保存 bk_switch 3 列、admin 保存 2 列，读取互不串档 ✓ |
| 筛选收藏按用户隔离 | tom 保存 1 条、admin 查询为空 ✓ |
| 创建者打标 | tom 创建实例 `creator = tom` ✓ |
| 修改者打标 | admin 更新主机 `modifier = admin`；bk_switch 无该列，更新不报错 ✓ |
| UI 渲染（无头浏览器） | tom 进入 `#/resource/instance/bk_switch`，星标按钮 + 列配置按钮均渲染，下拉展开正常，无运行时错误、无 404 ✓ |

## 四、已知小缺口

- **host 列表的「新增收藏条件」**：`host-list-options.vue` 当前未向 `filter-collection` 透传 `currentConditions`，因此 host 场景下点「新增收藏条件」会提示「请先设置筛选条件」再收藏（通用模型已完整支持）。如需 host 也完整，需要把 host 高级筛选的 conditionMap 透传进来——属于增量工作，未在本轮处理。
- host 列表的收藏「应用（apply）」事件上游 `host-list.vue` 本就未接（`collection-apply` 无人消费），属既有问题，本轮未改动其链路。
